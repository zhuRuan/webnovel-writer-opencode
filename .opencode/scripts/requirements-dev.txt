# Webnovel Writer - Dev Dependencies (optional)
# 安装: pip install -r requirements-dev.txt

pytest>=7.0.0           # 单元测试框架
pytest-cov>=4.1.0       # 覆盖率统计
pytest-asyncio>=0.23.0  # 异步测试支持
pytest-timeout>=2.3.0   # 测试超时保护
